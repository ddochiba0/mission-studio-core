{
  "name": "mission-studio-core",
  "version": "2.0.0-alpha.1",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "scripts": {
    "build": "pnpm -r build",
    "check:env-example": "node scripts/check-environment.mjs apps/studio-web/.env.example",
    "check:env-local": "node scripts/check-environment.mjs apps/studio-web/.env.local --require-values",
    "dev": "pnpm --filter @mission-studio/studio-web dev",
    "test": "pnpm -r test && node --test scripts/*.test.mjs",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
